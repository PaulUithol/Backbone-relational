language: node_js
node_js:
  - "4"
before_install:
  - npm install -g npm
  - npm install -g karma-cli
script:
  - npm test
  - $(npm bin)/karma start --single-run --browsers PhantomJS --lodash
sudo: false
cache:
  directories:
    - node_modules
